package com.schedule.demo.repository;

import com.schedule.demo.entity.SpotRequiredSkillSet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SpotRequiredSkillSetRepository extends JpaRepository<SpotRequiredSkillSet, Long> {
}
