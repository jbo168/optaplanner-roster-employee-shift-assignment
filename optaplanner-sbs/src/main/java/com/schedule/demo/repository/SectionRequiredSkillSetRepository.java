package com.schedule.demo.repository;

import com.schedule.demo.entity.SectionRequiredSkillSet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SectionRequiredSkillSetRepository extends JpaRepository<SectionRequiredSkillSet, Long> {
}
